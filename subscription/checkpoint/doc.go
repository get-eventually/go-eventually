// Package checkpoint expose the Checkpointer interface, used to checkpoint,
// or save, the current progress of a Subscription, so that it might survive
// application restarts without reprocessing Events.
package checkpoint
