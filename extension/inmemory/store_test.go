package inmemory_test

import (
	"testing"
)

func TestStoreSuite(t *testing.T) {
	// TODO(ar3s3ru): enable me again!
	// suite.Run(t, eventstore.NewStoreSuite(func() eventstore.Store {
	// 	return inmemory.NewEventStore()
	// }))
}
